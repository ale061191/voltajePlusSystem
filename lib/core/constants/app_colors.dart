import 'package:flutter/material.dart';

class AppColors {
  // Brand Identity (Cyberpunk Minimal)
  static const Color neonCyan = Color(0xFF00E5FF); // Primary Action
  static const Color neonGreen = Color(
    0xFF06F906,
  ); // Pure Neon Green (Stitch Design)
  static const Color neonRed = Color(0xFFFF0055); // Error / Stop

  // Background Surfaces
  static const Color backgroundBlack = Color(0xFF050505); // Global BG
  static const Color surfaceDark = Color(0xFF121212); // Cards / Sheets
  static const Color surfaceLight = Color(0xFF1E1E1E); // Hover / Input

  // Typography
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color textDisabled = Color(0xFF666666);

  // Overlay
  static const Color glassBlack = Color(0xCC000000); // 80% opacity
}
